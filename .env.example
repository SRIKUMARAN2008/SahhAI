# Server Config
PORT=5000
# LLM Configuration
# Provide an OpenAI API Key to activate the real LLM-backed agent (e.g. tool calling).
# If left empty, the server defaults to a custom local NLU/Regex matching engine.
OPENAI_API_KEY=
